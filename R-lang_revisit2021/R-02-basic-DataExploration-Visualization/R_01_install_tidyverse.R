# using function `install.packages(<package>)` to install 3rd party packages;
install.packages("tidyverse")
# using `library(<package>)` to load 3rd party package;
library("tidyverse")
library("readr")
# src file
src_file <- "R-00-exercise/Data/Crime_Data.csv"

# load

dfCrime <- read_csv
