# This is the reason I learn R lol ----
# R has plotting packages: ggplot2, ggvis, lattice

# source data
library(datatsets)
head(iris)
tail(iris)
summary(iris)

# plot() ----
plot(iris)


# hist() ----
#hist(iris)
